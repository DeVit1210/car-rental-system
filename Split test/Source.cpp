#include<iostream>
#include<fstream>
#include<sstream>
#include<string>
#include<vector>
#include<Windows.h>

using namespace std;

int main() {
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	/*ofstream fout("file.txt");
	fout << "I love Liza so much";
	fout.close();
	return 0;*/
	/*char separator = ' ';
	vector<string> vec;
	while (getline(stream, tempString, separator)) {
		vec.push_back(tempString);
	}
	for (const auto& elem : vec) {
		cout << elem << endl;
	}*/
	return 0;
}